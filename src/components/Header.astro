---
const { total = 0 } = Astro.props;
---
<header class="sticky top-0 z-50 bg-white/80 backdrop-blur border-b border-gray-200">
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center gap-4">
    <a href="/" class="font-bold tracking-tight text-xl">DEMO</a>
    <div class="flex-1"></div>
    <div id="searchBox" class="relative w-full max-w-md">
      <input id="searchInput" type="search" placeholder="Buscar productos..." class="w-full rounded-xl border border-gray-300 px-4 py-2 focus:outline-none focus:ring-2 focus:ring-sky-400" />
    </div>
    <button id="openCart" class="relative px-3 py-2 rounded-xl bg-sky-500 text-white">
      Carrito
      <!-- <span id="cartTotal" class="ml-2 font-semibold hidden">$${total}</span>
    </button> -->
  </div>
</header>
